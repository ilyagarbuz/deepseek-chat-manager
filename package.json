{
  "name": "deepseek_manager_extension",
  "version": "1.1.3",
  "main": "index.js",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && npm run prepare-dist",
    "prepare-dist": "cp manifest.json dist/ && cp -r icons dist/ && cp src/content/content.css dist/content.css",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../deepseek-chat-manager.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Browser extension for organizing DeepSeek chats into folders",
  "dependencies": {
    "@types/chrome": "^0.1.12",
    "@vitejs/plugin-vue": "^6.0.1",
    "lucide-vue-next": "^0.544.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.6",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "terser": "^5.44.0"
  }
}
