{
  "name": "deepseek_manager_extension",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && npm run prepare-dist",
    "prepare-dist": "cp manifest.json dist/ && cp -r icons dist/ && cp src/content/content.css dist/content.css",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "package": "npm run build && zip -r deepseek-manager-extension.zip dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Браузерное расширение для группировки чатов DeepSeek по папкам",
  "dependencies": {
    "@types/chrome": "^0.1.12",
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.6",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@types/node": "^24.5.2"
  }
}
